
import os

# Create the project directory structure
base_dir = "/mnt/agents/output/morocco-travel"
os.makedirs(base_dir, exist_ok=True)

# Create subdirectories for cities and assets
cities = ["marrakech", "essaouira", "chefchaouen"]
for city in cities:
    os.makedirs(os.path.join(base_dir, city), exist_ok=True)

print("Directory structure created successfully!")
print(os.listdir(base_dir))
